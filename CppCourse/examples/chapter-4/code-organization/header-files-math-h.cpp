// math.h — заголовочный файл (только объявления)
// В реальном проекте это был бы отдельный файл math.h
// Здесь показываем содержимое для демонстрации

#pragma once  // или #ifndef MATH_H / #define MATH_H / #endif

// Только объявления — никаких определений!
int add(int a, int b);
int multiply(int a, int b);
double circleArea(double radius);

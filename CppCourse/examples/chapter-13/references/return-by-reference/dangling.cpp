// ОПАСНЫЙ КОД — только для демонстрации!
// Никогда не возвращайте ссылку на локальную переменную

const int& getBad()
{const int local {42}; // локальная переменная
    return local; // ОШИБКА: local уничтожается при выходе из функции!}                 // Возвращённая ссылка — висячая (dangling reference)

int main()
{// const int& ref {getBad()}; // неопределённое поведение!
    return 0;}


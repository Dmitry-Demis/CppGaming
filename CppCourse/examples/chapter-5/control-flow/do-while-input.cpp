#include <iostream>

int main()
{// do-while для валидации ввода: запрашиваем число от 1 до 10
    // Тело выполняется хотя бы раз — пользователь всегда видит запрос
    int value {};
    do
    {std::cout << "Введите число от 1 до 10: ";
        std::cin >> value;} while (value < 1 || value > 10);

    std::cout << "Вы ввели: " << value << "\n";

    // Сравнение: while потребовал бы дублирования запроса или флага
    // Вариант с while (менее удобен):
    // int v {};
    // std::cout << "Введите число от 1 до 10: ";
    // std::cin >> v;
    // while (v < 1 || v > 10)
    // {//     std::cout << "Введите число от 1 до 10: ";
    //     std::cin >> v;
    //}

    return 0;}


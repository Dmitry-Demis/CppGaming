#include <iostream>

// Прямолинейная программа: путь выполнения всегда одинаков.
// CPU идёт сверху вниз, каждая строка выполняется ровно один раз.

int main()
{std::cout << "Введите целое число: ";

    int x{};
    std::cin >> x;

    std::cout << "Вы ввели: " << x << '\n';

    return 0;
    // Путь выполнения: строки 8, 10, 11, 13 — всегда в одном порядке.
    // Изменить этот порядок можно только операторами управления потоком.}

